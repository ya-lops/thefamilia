---
const flow = [
  {
    name: "Real job in 10 mins",
    text: "If your brain is tired, it rejects new information. Therefore, the job lasts a maximum of 10 minutes, but in an intensive format.",
  },
  {
    name: "No contracts",
    text: "We don't need to waste time on contracts. More than 60% of mates “bitch out” precisely for this reason. The work requires no preparation, and there is no obligation to a mentor.",
  },
  {
    name: "Up to 3 mentors",
    text: "To remove the barrier and understand different aspects of the business, 2-3 mentors, including real Capos.",
  },
  {
    name: "You decide how many jobs",
    text: "You can rob stores, steal cars. We will adapt to your wishes and schedule. But we recommend at least 3 jobs per week",
  },
];

const data = flow;
---

<ul class="app-how-list">
  {
    data.map((item) => (
      <li class="_item">
        <div class="_name">{item.name}</div>
        <div class="_text" set:html={item.text} />
      </li>
    ))
  }
</ul>

<style lang="scss">
  .app-how-list {
    list-style: none;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    grid-auto-rows: minmax(0, 1fr);
    gap: 4.8rem;

    @media (width < 960px) {
      grid-template-columns: minmax(0, 1fr);
      grid-auto-rows: unset;
    }

    ._item {
      @media (width > 960px) {
        &:nth-child(2n + 1) {
          display: flex;
          flex-flow: column;
          align-items: flex-end;
          text-align: right;
        }
      }
    }

    ._name {
      font-size: 3.2rem;
      font-weight: 500;
      color: var(--color-accent);
      margin-bottom: 1rem;
      font-family: var(--font-family-secondary);
      text-wrap: balance;
    }

    ._text {
      line-height: 1.5;
      text-wrap: balance;
    }
  }
</style>
